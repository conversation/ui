language: node_js
node_js:
  - "8"
env:
  global:
    - PATH=$HOME/.local/bin:$PATH
before_install: pip install --user awscli
deploy:
  on:
    branch: master
  provider: script
  script: make release
  skip_cleanup: true
